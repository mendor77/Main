# Main
echo "# Main" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/mendor77/Main.git
git push -u origin main
Make sure you generate a access token and paste in into the password
